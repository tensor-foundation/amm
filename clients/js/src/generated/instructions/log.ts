/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import { Address } from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import {
  IAccountMeta,
  IInstruction,
  IInstructionWithAccounts,
  IInstructionWithData,
  ReadonlyAccount,
} from '@solana/instructions';
import { AMM_PROGRAM_ADDRESS } from '../programs';
import { ResolvedAccount, getAccountMetaFactory } from '../shared';

export type LogInstruction<
  TProgram extends string = typeof AMM_PROGRAM_ADDRESS,
  TAccountPool extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountPool extends string
        ? ReadonlyAccount<TAccountPool>
        : TAccountPool,
      ...TRemainingAccounts,
    ]
  >;

export type LogInstructionData = {
  discriminator: Array<number>;
  event: Uint8Array;
};

export type LogInstructionDataArgs = { event: Uint8Array };

export function getLogInstructionDataEncoder(): Encoder<LogInstructionDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getArrayEncoder(getU8Encoder(), { size: 8 })],
      ['event', getBytesEncoder({ size: getU32Encoder() })],
    ]),
    (value) => ({
      ...value,
      discriminator: [141, 230, 214, 242, 9, 209, 207, 170],
    })
  );
}

export function getLogInstructionDataDecoder(): Decoder<LogInstructionData> {
  return getStructDecoder([
    ['discriminator', getArrayDecoder(getU8Decoder(), { size: 8 })],
    ['event', getBytesDecoder({ size: getU32Decoder() })],
  ]);
}

export function getLogInstructionDataCodec(): Codec<
  LogInstructionDataArgs,
  LogInstructionData
> {
  return combineCodec(
    getLogInstructionDataEncoder(),
    getLogInstructionDataDecoder()
  );
}

export type LogInput<TAccountPool extends string = string> = {
  pool: Address<TAccountPool>;
  event: LogInstructionDataArgs['event'];
};

export function getLogInstruction<TAccountPool extends string>(
  input: LogInput<TAccountPool>
): LogInstruction<typeof AMM_PROGRAM_ADDRESS, TAccountPool> {
  // Program address.
  const programAddress = AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.pool)],
    programAddress,
    data: getLogInstructionDataEncoder().encode(args as LogInstructionDataArgs),
  } as LogInstruction<typeof AMM_PROGRAM_ADDRESS, TAccountPool>;

  return instruction;
}

export type ParsedLogInstruction<
  TProgram extends string = typeof AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
  };
  data: LogInstructionData;
};

export function parseLogInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedLogInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
    },
    data: getLogInstructionDataDecoder().decode(instruction.data),
  };
}
